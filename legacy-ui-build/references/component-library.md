# Component Library

Document the components available in the target React project.

## shadcn/ui Setup

- shadcn installed: yes/no
- `components.json` path:
- component alias:
- utils path:
- Tailwind config path:
- CSS variables path:
- package manager:

## Installed shadcn Components

List installed components and import paths.

```txt
button:
card:
table:
tabs:
dialog:
drawer:
sheet:
badge:
input:
select:
checkbox:
dropdown-menu:
separator:
alert:
toast/sonner:
```

## Client-Wrapped Components

If the client already wraps shadcn components, document them here.

```txt
Component:
Import:
Use when:
Avoid when:
```

## Installation Policy

- Can the agent run shadcn CLI to add missing components? yes/no
- Preferred command:
- Components that should not be installed:

## Import Conventions

- React import style:
- Path aliases:
- CSS module or Tailwind:
- Server/client component rules:

## Data And State Conventions

- Mock data allowed: yes/no
- API hooks:
- Form library:
- Table library:
- Validation library:
- State management:
